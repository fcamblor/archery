assistant: claude

worktree:
  baseBranch: main
  copyFiles:
    - .env
    - .env.local

defaults:
  loadDefaultCommands: true
  loadDefaultWorkflows: true

impl-validation-command: "pnpm run build"
